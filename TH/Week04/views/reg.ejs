<%- include('header'); -%>
<%- include('navbar'); -%>

<style>
  #example2 {
    border: 1px solid;
    padding: 10px;
    box-shadow: 5px 10px #888888;
  }
</style>
<br>
<h3>Danh sách các hội nghị để bạn đăng kí</h3>
<table class="table">
    <thead>
      <tr>
        <th scope="col">id</th>
        <th scope="col">Name</th>
        <th scope="col">Detail</th>
        <th scope="col">Time started</th>
      </tr>
    </thead>
     <tbody>
      <% if(confList) { %>
          <% confList.forEach(conf => { %>
        <tr>
          <td><%= conf.id %></td>
          <td><%= conf.name %></td>
          <td><%= conf.detail %></td>
          <td><%= conf.timeStarted %></td>
        </tr>

      <% })} %>
      <% %>

      </tbody>
</table>

<form action="/reg" method="POST">
    <script>
        function isChecked(chk,sub1) {
        var myLayer = document.getElementById(sub1);
        if (chk.checked == true) {
            myLayer.disabled = false;
        } else {
            myLayer.disabled = true;
        }}

       
   </script>
    <br>
    <table id ="table1">
      <div class="form-group" id = "example2">
        <label for="text">Id của hội nghị</label>
        <input type="text" placeholder="id" class="form-control" id="idConference" name="idConference">
      </div>
      <div class="form-group" id = "example2">
        <label for="text">Name</label>
        <input type="text" placeholder="Name" class="form-control" id="Name" name="Name">
      </div>
      <div class="form-group" id = "example2">
        <label for="Email">Email</label>
        <input type="email" placeholder="Email" class="form-control" id="Email" name="Email">
      </div>  
      <div class="form-group" id = "example2">
        <label for="phone">Phone number</label>
        <input type="number" placeholder="phone" class="form-control" id="phone" name="phone">
      </div>  
      
      <h2> <u>Will you attend?</u></h2>
      <input type="checkbox" id="termsChkbx " onchange="isChecked(this,'sub1')"/></p>
      <h3><input type="submit" name="submit" value="Sign up" id="sub1" disabled="disabled" /></h3>
       <a href="/registeraccount" class="btn btn-info">Create an account</a>
    </table>
    </form>
<%- include('footer'); -%>